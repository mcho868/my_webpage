<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Brendan Choi // AI Engineer</title>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,100..800;1,100..800&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #080b0f;
    --bg2: #0d1117;
    --bg3: #111820;
    --green: #39ff6b;
    --green-dim: #1a7a35;
    --cyan: #00d4ff;
    --cyan-dim: #0a4a5a;
    --amber: #ffb347;
    --red: #ff4757;
    --white: #e2e8f0;
    --muted: #4a5568;
    --border: #1e2a38;
    --glow-green: 0 0 10px rgba(57,255,107,0.4), 0 0 30px rgba(57,255,107,0.15);
    --glow-cyan: 0 0 10px rgba(0,212,255,0.4), 0 0 30px rgba(0,212,255,0.15);
    --font-mono: 'JetBrains Mono', 'Space Mono', monospace;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--white);
    font-family: var(--font-mono);
    font-size: 14px;
    line-height: 1.6;
    overflow-x: hidden;
    cursor: none;
  }

  /* Custom cursor */
  #cursor {
    position: fixed;
    width: 10px;
    height: 18px;
    background: var(--green);
    z-index: 9999;
    pointer-events: none;
    animation: blink 1s step-end infinite;
    mix-blend-mode: screen;
  }

  @keyframes blink { 50% { opacity: 0; } }

  /* Scanline overlay */
  body::before {
    content: '';
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0,0,0,0.08) 2px,
      rgba(0,0,0,0.08) 4px
    );
    pointer-events: none;
    z-index: 1000;
  }

  /* Noise overlay */
  body::after {
    content: '';
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 999;
    opacity: 0.4;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 500;
    padding: 12px 40px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(8,11,15,0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }

  .nav-logo {
    color: var(--green);
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-decoration: none;
  }

  .nav-logo span { color: var(--muted); }

  nav ul {
    display: flex;
    gap: 32px;
    list-style: none;
  }

  nav a {
    color: var(--muted);
    text-decoration: none;
    font-size: 12px;
    letter-spacing: 0.08em;
    transition: color 0.2s;
    position: relative;
  }

  nav a::before {
    content: '> ';
    color: var(--green);
    opacity: 0;
    transition: opacity 0.2s;
  }

  nav a:hover { color: var(--green); }
  nav a:hover::before { opacity: 1; }

  /* HERO */
  #hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    padding: 100px 80px 60px;
    gap: 60px;
    position: relative;
  }

  .hero-left { position: relative; }

  .hero-prompt {
    color: var(--green);
    font-size: 12px;
    margin-bottom: 8px;
    opacity: 0;
    animation: fadeUp 0.6s 0.3s forwards;
  }

  .hero-title {
    font-size: clamp(28px, 4vw, 52px);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: 24px;
    opacity: 0;
    animation: fadeUp 0.6s 0.5s forwards;
  }

  .hero-title .line1 { color: var(--white); display: block; }
  .hero-title .line2 { color: var(--green); display: block; }
  .hero-title .line3 { color: var(--cyan); display: block; font-style: italic; }

  .hero-subtitle {
    color: var(--muted);
    font-size: 13px;
    line-height: 1.8;
    margin-bottom: 40px;
    max-width: 420px;
    opacity: 0;
    animation: fadeUp 0.6s 0.7s forwards;
  }

  .hero-subtitle .highlight { color: var(--green); }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 40px;
    opacity: 0;
    animation: fadeUp 0.6s 0.9s forwards;
  }

  .tag {
    padding: 4px 12px;
    border: 1px solid var(--border);
    border-radius: 2px;
    font-size: 11px;
    color: var(--muted);
    background: var(--bg2);
    letter-spacing: 0.06em;
    transition: all 0.2s;
  }

  .tag:hover {
    border-color: var(--green);
    color: var(--green);
    box-shadow: var(--glow-green);
    background: rgba(57,255,107,0.05);
  }

  .hero-ctas {
    display: flex;
    gap: 16px;
    opacity: 0;
    animation: fadeUp 0.6s 1.1s forwards;
  }

  .btn-primary {
    padding: 10px 24px;
    background: var(--green);
    color: var(--bg);
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.1em;
    border: none;
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    transition: all 0.2s;
    clip-path: polygon(0 0, calc(100% - 8px) 0, 100% 8px, 100% 100%, 8px 100%, 0 calc(100% - 8px));
  }

  .btn-primary:hover {
    box-shadow: var(--glow-green);
    transform: translateY(-2px);
  }

  .btn-outline {
    padding: 10px 24px;
    background: transparent;
    color: var(--cyan);
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.1em;
    border: 1px solid var(--cyan-dim);
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    transition: all 0.2s;
  }

  .btn-outline:hover {
    border-color: var(--cyan);
    box-shadow: var(--glow-cyan);
    background: rgba(0,212,255,0.05);
  }

  /* ASCII art panel */
  .hero-right {
    display: flex;
    flex-direction: column;
    gap: 16px;
    opacity: 0;
    animation: fadeLeft 0.8s 0.8s forwards;
  }

  .terminal-window {
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: 6px;
    overflow: hidden;
    position: relative;
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px 16px;
    background: var(--bg3);
    border-bottom: 1px solid var(--border);
  }

  .dot { width: 10px; height: 10px; border-radius: 50%; }
  .dot.red { background: #ff5f56; }
  .dot.yellow { background: #ffbd2e; }
  .dot.green { background: #27c93f; }

  .terminal-title {
    margin-left: 8px;
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 0.06em;
  }

  .terminal-body {
    padding: 20px;
    font-size: 12px;
    line-height: 1.7;
  }

  .ascii-art {
    color: var(--green);
    font-size: 10px;
    line-height: 1.2;
    white-space: pre;
    text-shadow: var(--glow-green);
    overflow: hidden;
  }

  .cmd-line { display: flex; gap: 8px; align-items: flex-start; margin: 4px 0; }
  .prompt { color: var(--green); flex-shrink: 0; }
  .cmd { color: var(--cyan); }
  .output { color: var(--white); padding-left: 16px; }
  .output-dim { color: var(--muted); padding-left: 16px; font-size: 11px; }
  .comment { color: var(--muted); font-style: italic; }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeLeft {
    from { opacity: 0; transform: translateX(30px); }
    to { opacity: 1; transform: translateX(0); }
  }

  /* SECTION STYLES */
  section { padding: 80px; }

  .section-header {
    margin-bottom: 60px;
    display: flex;
    align-items: center;
    gap: 24px;
  }

  .section-num {
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 0.1em;
  }

  .section-title {
    font-size: 28px;
    font-weight: 700;
    color: var(--white);
    letter-spacing: -0.02em;
  }

  .section-title .accent { color: var(--green); }

  .section-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, var(--border), transparent);
  }

  /* ASCII DIVIDER */
  .ascii-divider {
    text-align: center;
    color: var(--muted);
    font-size: 10px;
    letter-spacing: 0.2em;
    padding: 8px 0;
    opacity: 0.4;
  }

  /* ABOUT */
  #about { background: var(--bg); }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
  }

  .about-bio p {
    color: var(--white);
    font-size: 14px;
    line-height: 1.9;
    margin-bottom: 20px;
    opacity: 0.85;
  }

  .about-bio p span.g { color: var(--green); }
  .about-bio p span.c { color: var(--cyan); }
  .about-bio p span.a { color: var(--amber); }

  .stat-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-top: 32px;
  }

  .stat-card {
    padding: 20px;
    border: 1px solid var(--border);
    background: var(--bg2);
    position: relative;
    overflow: hidden;
    transition: all 0.3s;
  }

  .stat-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 3px; height: 100%;
    background: var(--green);
  }

  .stat-card:hover { border-color: var(--green-dim); transform: translateY(-2px); }

  .stat-num {
    font-size: 32px;
    font-weight: 800;
    color: var(--green);
    line-height: 1;
    margin-bottom: 4px;
  }

  .stat-label { font-size: 11px; color: var(--muted); letter-spacing: 0.08em; }

  /* SKILLS */
  #skills { background: var(--bg2); }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }

  .skill-box {
    border: 1px solid var(--border);
    padding: 24px 20px;
    background: var(--bg);
    position: relative;
    transition: all 0.3s;
  }

  .skill-box::after {
    content: attr(data-corner);
    position: absolute;
    bottom: 8px; right: 10px;
    font-size: 9px;
    color: var(--muted);
    opacity: 0.3;
  }

  .skill-box:hover {
    border-color: var(--cyan-dim);
    background: rgba(0,212,255,0.02);
  }

  .skill-box-header {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--border);
  }

  .skill-icon { font-size: 16px; }

  .skill-box-title {
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--cyan);
  }

  .skill-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin: 8px 0;
    font-size: 12px;
    color: var(--white);
    opacity: 0.8;
  }

  .skill-bar {
    height: 2px;
    background: var(--border);
    flex: 1;
    margin-left: 12px;
    position: relative;
    overflow: hidden;
  }

  .skill-bar-fill {
    height: 100%;
    background: linear-gradient(to right, var(--green), var(--cyan));
    transform-origin: left;
    animation: barGrow 1.2s ease forwards;
    transform: scaleX(0);
  }

  @keyframes barGrow { to { transform: scaleX(1); } }

  /* PROJECTS */
  #projects { background: var(--bg); }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .project-card {
    border: 1px solid var(--border);
    background: var(--bg2);
    overflow: hidden;
    position: relative;
    transition: all 0.3s;
    cursor: pointer;
  }

  .project-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(to right, var(--green), var(--cyan));
    transform: scaleX(0);
    transition: transform 0.3s;
    transform-origin: left;
  }

  .project-card:hover::before { transform: scaleX(1); }
  .project-card:hover { border-color: var(--green-dim); transform: translateY(-4px); }

  .project-card-header {
    padding: 20px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .project-type {
    font-size: 10px;
    color: var(--green);
    letter-spacing: 0.12em;
    font-weight: 700;
  }

  .project-status {
    font-size: 10px;
    padding: 2px 8px;
    border-radius: 1px;
  }

  .status-active { background: rgba(57,255,107,0.1); color: var(--green); border: 1px solid var(--green-dim); }
  .status-build { background: rgba(255,179,71,0.1); color: var(--amber); border: 1px solid rgba(255,179,71,0.3); }
  .status-done { background: rgba(0,212,255,0.1); color: var(--cyan); border: 1px solid var(--cyan-dim); }

  .project-body { padding: 20px; }

  .project-title {
    font-size: 16px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 8px;
    letter-spacing: -0.01em;
  }

  .project-desc {
    font-size: 12px;
    color: var(--muted);
    line-height: 1.7;
    margin-bottom: 16px;
  }

  .project-stack {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .stack-tag {
    font-size: 10px;
    padding: 2px 8px;
    border: 1px solid var(--border);
    color: var(--muted);
    letter-spacing: 0.06em;
    background: var(--bg);
  }

  /* EXPERIENCE / GIT LOG */
  #experience { background: var(--bg2); }

  .git-log {
    position: relative;
    padding-left: 40px;
  }

  .git-log::before {
    content: '';
    position: absolute;
    left: 8px; top: 0; bottom: 0;
    width: 1px;
    background: linear-gradient(to bottom, var(--green), transparent);
  }

  .git-entry {
    position: relative;
    margin-bottom: 40px;
    opacity: 0;
    transform: translateX(-20px);
    transition: all 0.6s;
  }

  .git-entry.visible {
    opacity: 1;
    transform: translateX(0);
  }

  .git-entry::before {
    content: '';
    position: absolute;
    left: -36px; top: 6px;
    width: 10px; height: 10px;
    border-radius: 50%;
    background: var(--bg2);
    border: 2px solid var(--green);
    box-shadow: var(--glow-green);
  }

  .git-hash {
    font-size: 11px;
    color: var(--amber);
    letter-spacing: 0.08em;
    margin-bottom: 6px;
    display: flex;
    gap: 16px;
    align-items: center;
  }

  .git-date { color: var(--muted); }

  .git-title {
    font-size: 16px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 4px;
  }

  .git-org {
    font-size: 12px;
    color: var(--cyan);
    margin-bottom: 8px;
  }

  .git-body {
    font-size: 12px;
    color: var(--muted);
    line-height: 1.7;
    max-width: 600px;
  }

  /* TERMINAL SECTION */
  #terminal-demo {
    background: var(--bg);
    padding: 80px;
  }

  .big-terminal {
    max-width: 800px;
    margin: 0 auto;
  }

  #term-output {
    font-size: 13px;
    line-height: 1.8;
    min-height: 300px;
    max-height: 400px;
    overflow-y: auto;
  }

  #term-output::-webkit-scrollbar { width: 4px; }
  #term-output::-webkit-scrollbar-track { background: var(--bg3); }
  #term-output::-webkit-scrollbar-thumb { background: var(--green-dim); }

  .term-input-row {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 20px;
    border-top: 1px solid var(--border);
    background: var(--bg3);
  }

  .term-input-row .prompt { color: var(--green); flex-shrink: 0; font-size: 13px; }

  #term-input {
    flex: 1;
    background: transparent;
    border: none;
    outline: none;
    color: var(--white);
    font-family: var(--font-mono);
    font-size: 13px;
    caret-color: var(--green);
  }

  /* CONTACT */
  #contact {
    background: var(--bg2);
    text-align: center;
  }

  .contact-ascii {
    color: var(--green);
    font-size: 10px;
    line-height: 1.3;
    white-space: pre;
    text-shadow: var(--glow-green);
    margin: 0 auto 40px;
    display: inline-block;
  }

  .contact-links {
    display: flex;
    justify-content: center;
    gap: 32px;
    flex-wrap: wrap;
  }

  .contact-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    color: var(--muted);
    text-decoration: none;
    font-size: 12px;
    letter-spacing: 0.08em;
    transition: all 0.2s;
  }

  .contact-link:hover { color: var(--green); }

  .contact-link-icon {
    width: 48px; height: 48px;
    border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
    transition: all 0.2s;
  }

  .contact-link:hover .contact-link-icon {
    border-color: var(--green);
    box-shadow: var(--glow-green);
    background: rgba(57,255,107,0.05);
  }

  /* FOOTER */
  footer {
    padding: 24px 80px;
    border-top: 1px solid var(--border);
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 11px;
    color: var(--muted);
  }

  /* MATRIX EASTER EGG */
  #matrix-canvas {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: 998;
    display: none;
  }

  .matrix-active #matrix-canvas { display: block; }

  .matrix-exit {
    position: fixed;
    bottom: 40px; left: 50%;
    transform: translateX(-50%);
    z-index: 10000;
    display: none;
    padding: 8px 20px;
    border: 1px solid var(--green);
    background: var(--bg2);
    color: var(--green);
    font-family: var(--font-mono);
    font-size: 12px;
    cursor: pointer;
    box-shadow: var(--glow-green);
  }

  .matrix-active .matrix-exit { display: block; }

  /* GLITCH EFFECT */
  @keyframes glitch {
    0% { transform: translate(0); }
    20% { transform: translate(-2px, 1px); clip-path: inset(20% 0 30% 0); }
    40% { transform: translate(2px, -1px); clip-path: inset(60% 0 10% 0); }
    60% { transform: translate(0); clip-path: none; }
    80% { transform: translate(-1px, 2px); clip-path: inset(40% 0 40% 0); }
    100% { transform: translate(0); clip-path: none; }
  }

  .glitch { position: relative; }
  .glitch:hover { animation: glitch 0.4s; }

  /* SCROLL ANIMATION */
  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: all 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Fun blinking cursor in terminal */
  .typing::after {
    content: '▊';
    color: var(--green);
    animation: blink 0.8s step-end infinite;
  }

  /* Responsive */
  @media (max-width: 900px) {
    #hero { grid-template-columns: 1fr; padding: 100px 40px 60px; }
    .hero-right { display: none; }
    section { padding: 60px 40px; }
    .skills-grid { grid-template-columns: 1fr 1fr; }
    .projects-grid { grid-template-columns: 1fr; }
    .about-grid { grid-template-columns: 1fr; }
    footer { flex-direction: column; gap: 12px; text-align: center; }
  }
</style>
</head>
<body>

<div id="cursor"></div>
<canvas id="matrix-canvas"></canvas>
<button class="matrix-exit" onclick="exitMatrix()">[ press ESC to exit matrix ]</button>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">brendan<span>@</span>choi <span style="color:var(--green)">~</span></a>
  <ul>
    <li><a href="#about">about</a></li>
    <li><a href="#skills">skills</a></li>
    <li><a href="#projects">projects</a></li>
    <li><a href="#experience">experience</a></li>
    <li><a href="#terminal-demo">terminal</a></li>
    <li><a href="#contact">contact</a></li>
  </ul>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-left">
    <div class="hero-prompt">// fullstack AI engineer · Auckland, NZ</div>
    <h1 class="hero-title glitch">
      <span class="line1">I TURN IDEAS</span>
      <span class="line2">INTO INTELLIGENT</span>
      <span class="line3">SYSTEMS.</span>
    </h1>
    <p class="hero-subtitle">
      Building <span class="highlight">AI-powered applications</span> at the intersection of
      machine learning, full-stack dev, and real-world impact.
      From on-device medical AI to fintech systems — if it's complex, it's interesting.
    </p>
    <div class="hero-tags">
      <span class="tag">#python</span>
      <span class="tag">#pytorch</span>
      <span class="tag">#react</span>
      <span class="tag">#llm</span>
      <span class="tag">#nextjs</span>
      <span class="tag">#ml-ops</span>
      <span class="tag">#nz-based</span>
    </div>
    <div class="hero-ctas">
      <a href="#projects" class="btn-primary">VIEW PROJECTS</a>
      <a href="#contact" class="btn-outline">GET IN TOUCH</a>
    </div>
  </div>

  <div class="hero-right">
    <div class="terminal-window">
      <div class="terminal-bar">
        <div class="dot red"></div>
        <div class="dot yellow"></div>
        <div class="dot green"></div>
        <span class="terminal-title">~/whoami.sh</span>
      </div>
      <div class="terminal-body">
        <pre class="ascii-art">
 ██████╗ ██████╗ ███████╗███╗   ██╗██████╗  █████╗ ███╗   ██╗
 ██╔══██╗██╔══██╗██╔════╝████╗  ██║██╔══██╗██╔══██╗████╗  ██║
 ██████╦╝██████╔╝█████╗  ██╔██╗ ██║██║  ██║███████║██╔██╗ ██║
 ██╔══██╗██╔══██╗██╔══╝  ██║╚██╗██║██║  ██║██╔══██║██║╚██╗██║
 ██████╦╝██║  ██║███████╗██║ ╚████║██████╔╝██║  ██║██║ ╚████║
 ╚═════╝ ╚═╝  ╚═╝╚══════╝╚═╝  ╚═══╝╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═══╝</pre>
        <div style="margin-top:16px">
          <div class="cmd-line">
            <span class="prompt">brendan@choi:~$</span>
            <span class="cmd">cat profile.json</span>
          </div>
          <div style="color:var(--muted); padding-left:16px; margin-top:4px; font-size:12px">
            <span style="color:var(--border)">{</span><br>
            &nbsp;&nbsp;<span style="color:var(--cyan)">"role"</span>: <span style="color:var(--amber)">"Fullstack AI Engineer"</span>,<br>
            &nbsp;&nbsp;<span style="color:var(--cyan)">"location"</span>: <span style="color:var(--amber)">"Auckland, NZ 🥝"</span>,<br>
            &nbsp;&nbsp;<span style="color:var(--cyan)">"gpa"</span>: <span style="color:var(--green)">8.0831</span>,<br>
            &nbsp;&nbsp;<span style="color:var(--cyan)">"status"</span>: <span style="color:var(--green)">"available_for_hire"</span>,<br>
            &nbsp;&nbsp;<span style="color:var(--cyan)">"interests"</span>: [<span style="color:var(--amber)">"fintech"</span>, <span style="color:var(--amber)">"healthcare_ai"</span>, <span style="color:var(--amber)">"web3"</span>]<br>
            <span style="color:var(--border)">}</span>
          </div>
        </div>
      </div>
    </div>

    <div class="terminal-window">
      <div class="terminal-bar">
        <div class="dot red"></div>
        <div class="dot yellow"></div>
        <div class="dot green"></div>
        <span class="terminal-title">~/status.log</span>
      </div>
      <div class="terminal-body">
        <div class="cmd-line">
          <span class="prompt">$</span>
          <span style="color:var(--green); font-size:12px">▶ Currently building:</span>
        </div>
        <div style="padding-left:16px; font-size:12px; margin-top:8px; display:flex; flex-direction:column; gap:8px;">
          <div style="display:flex; align-items:center; gap:10px">
            <span style="color:var(--amber)">●</span>
            <span style="color:var(--white)">AI booking automation (SaaS)</span>
          </div>
          <div style="display:flex; align-items:center; gap:10px">
            <span style="color:var(--green)">●</span>
            <span style="color:var(--white)">On-device medical triage LLM</span>
          </div>
          <div style="display:flex; align-items:center; gap:10px">
            <span style="color:var(--cyan)">●</span>
            <span style="color:var(--white)">DRL crypto trading research</span>
          </div>
        </div>
        <div style="margin-top:16px; padding-top:12px; border-top:1px solid var(--border); font-size:11px; color:var(--muted)">
          <span style="color:var(--green)">✓</span> Thesis submitted · <span style="color:var(--amber)">open to roles</span>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="ascii-divider">─────────────────────── // ───────────────────────</div>

<!-- ABOUT -->
<section id="about">
  <div class="section-header reveal">
    <span class="section-num">01.</span>
    <h2 class="section-title"><span class="accent">about</span>_me</h2>
    <div class="section-line"></div>
  </div>
  <div class="about-grid">
    <div class="about-bio reveal">
      <p>
        I'm a <span class="g">Computer Science (Honours)</span> graduate from the
        University of Auckland, specialising in <span class="c">machine learning, large-scale
        software dev, and AI-powered applications</span>.
      </p>
      <p>
        Currently CTO at two startups — <span class="a">Alignwithblind.com</span> and Aialow.co —
        where I lead the integration of AI into products that actually scale.
      </p>
      <p>
        Beyond the keyboard, I'm a <span class="g">certified personal trainer</span>, electric
        guitarist, and fitness obsessive. That balance keeps the problem-solving sharp.
      </p>
      <div class="stat-grid">
        <div class="stat-card">
          <div class="stat-num">8.08</div>
          <div class="stat-label">CGPA / 9.0</div>
        </div>
        <div class="stat-card">
          <div class="stat-num">2x</div>
          <div class="stat-label">CTO ROLES</div>
        </div>
        <div class="stat-card">
          <div class="stat-num">5+</div>
          <div class="stat-label">YEARS CODING</div>
        </div>
        <div class="stat-card">
          <div class="stat-num">∞</div>
          <div class="stat-label">COFFEES CONSUMED</div>
        </div>
      </div>
    </div>

    <div class="terminal-window reveal">
      <div class="terminal-bar">
        <div class="dot red"></div><div class="dot yellow"></div><div class="dot green"></div>
        <span class="terminal-title">~/skills_overview.sh</span>
      </div>
      <div class="terminal-body" style="font-size:12px">
        <div class="cmd-line"><span class="prompt">$</span><span class="cmd">ls -la core_competencies/</span></div>
        <div style="margin-top:10px; display:flex; flex-direction:column; gap:6px; padding-left:16px; color:var(--muted)">
          <div><span style="color:var(--green)">drwxr-xr-x</span> ai-ml-engineering/</div>
          <div><span style="color:var(--green)">drwxr-xr-x</span> fullstack-web/</div>
          <div><span style="color:var(--green)">drwxr-xr-x</span> ios-mobile/</div>
          <div><span style="color:var(--cyan)">-rw-r--r--</span> python.exe <span style="color:var(--green)">★★★★★</span></div>
          <div><span style="color:var(--cyan)">-rw-r--r--</span> react.jsx <span style="color:var(--green)">★★★★★</span></div>
          <div><span style="color:var(--cyan)">-rw-r--r--</span> pytorch.py <span style="color:var(--green)">★★★★☆</span></div>
          <div><span style="color:var(--cyan)">-rw-r--r--</span> swift.swift <span style="color:var(--green)">★★★★☆</span></div>
          <div><span style="color:var(--cyan)">-rw-r--r--</span> solidity.sol <span style="color:var(--green)">★★★☆☆</span></div>
        </div>
        <div style="margin-top:16px" class="cmd-line"><span class="prompt">$</span><span class="cmd">uptime</span></div>
        <div style="padding-left:16px; color:var(--muted); margin-top:4px">up 24yrs, still <span style="color:var(--green)">building</span></div>
      </div>
    </div>
  </div>
</section>

<div class="ascii-divider">┌──────────────────────────────────────────────────────┐</div>
<div class="ascii-divider">│  &lt;  SKILLS MATRIX  /&gt;                                │</div>
<div class="ascii-divider">└──────────────────────────────────────────────────────┘</div>

<!-- SKILLS -->
<section id="skills">
  <div class="section-header reveal">
    <span class="section-num">02.</span>
    <h2 class="section-title"><span class="accent">skill</span>_tree.json</h2>
    <div class="section-line"></div>
  </div>
  <div class="skills-grid">
    <div class="skill-box reveal" data-corner="[AI]">
      <div class="skill-box-header">
        <span class="skill-icon">🤖</span>
        <span class="skill-box-title">ARTIFICIAL INTELLIGENCE</span>
      </div>
      <div class="skill-item">RAG Systems <div class="skill-bar"><div class="skill-bar-fill" style="width:92%"></div></div></div>
      <div class="skill-item">LLM Fine-tuning <div class="skill-bar"><div class="skill-bar-fill" style="width:88%"></div></div></div>
      <div class="skill-item">Deep RL <div class="skill-bar"><div class="skill-bar-fill" style="width:82%"></div></div></div>
      <div class="skill-item">Computer Vision <div class="skill-bar"><div class="skill-bar-fill" style="width:75%"></div></div></div>
      <div class="skill-item">NLP <div class="skill-bar"><div class="skill-bar-fill" style="width:85%"></div></div></div>
    </div>
    <div class="skill-box reveal" data-corner="[DEV]">
      <div class="skill-box-header">
        <span class="skill-icon">⚡</span>
        <span class="skill-box-title">FULLSTACK DEV</span>
      </div>
      <div class="skill-item">React / Next.js <div class="skill-bar"><div class="skill-bar-fill" style="width:95%"></div></div></div>
      <div class="skill-item">Python / Django <div class="skill-bar"><div class="skill-bar-fill" style="width:92%"></div></div></div>
      <div class="skill-item">Node.js <div class="skill-bar"><div class="skill-bar-fill" style="width:80%"></div></div></div>
      <div class="skill-item">PostgreSQL <div class="skill-bar"><div class="skill-bar-fill" style="width:78%"></div></div></div>
      <div class="skill-item">Docker / AWS <div class="skill-bar"><div class="skill-bar-fill" style="width:72%"></div></div></div>
    </div>
    <div class="skill-box reveal" data-corner="[MOB]">
      <div class="skill-box-header">
        <span class="skill-icon">📱</span>
        <span class="skill-box-title">MOBILE &amp; WEB3</span>
      </div>
      <div class="skill-item">Swift / iOS <div class="skill-bar"><div class="skill-bar-fill" style="width:80%"></div></div></div>
      <div class="skill-item">React Native <div class="skill-bar"><div class="skill-bar-fill" style="width:75%"></div></div></div>
      <div class="skill-item">Solidity <div class="skill-bar"><div class="skill-bar-fill" style="width:65%"></div></div></div>
      <div class="skill-item">Web3.js <div class="skill-bar"><div class="skill-bar-fill" style="width:60%"></div></div></div>
      <div class="skill-item">Smart Contracts <div class="skill-bar"><div class="skill-bar-fill" style="width:62%"></div></div></div>
    </div>
    <div class="skill-box reveal" data-corner="[OPS]">
      <div class="skill-box-header">
        <span class="skill-icon">🛠</span>
        <span class="skill-box-title">TOOLS &amp; OPS</span>
      </div>
      <div class="skill-item">Git / GitHub <div class="skill-bar"><div class="skill-bar-fill" style="width:95%"></div></div></div>
      <div class="skill-item">Linux / Bash <div class="skill-bar"><div class="skill-bar-fill" style="width:85%"></div></div></div>
      <div class="skill-item">Figma / UI <div class="skill-bar"><div class="skill-bar-fill" style="width:78%"></div></div></div>
      <div class="skill-item">Agile / Scrum <div class="skill-bar"><div class="skill-bar-fill" style="width:80%"></div></div></div>
      <div class="skill-item">CI/CD <div class="skill-bar"><div class="skill-bar-fill" style="width:70%"></div></div></div>
    </div>
  </div>
</section>

<!-- PROJECTS -->
<section id="projects">
  <div class="section-header reveal">
    <span class="section-num">03.</span>
    <h2 class="section-title"><span class="accent">git</span>_log --projects</h2>
    <div class="section-line"></div>
  </div>
  <div class="projects-grid">
    <div class="project-card reveal">
      <div class="project-card-header">
        <span class="project-type">HEALTHCARE AI</span>
        <span class="project-status status-done">SHIPPED</span>
      </div>
      <div class="project-body">
        <div class="project-title">OnDevice Medical Triage</div>
        <div class="project-desc">Privacy-preserving medical triage using SLMs with RAG + LoRA. Built for NZ telenursing with cultural considerations for Māori &amp; Pacific populations.</div>
        <div class="project-stack">
          <span class="stack-tag">Python</span><span class="stack-tag">PyTorch</span>
          <span class="stack-tag">LoRA</span><span class="stack-tag">RAG</span><span class="stack-tag">SLM</span>
        </div>
      </div>
    </div>
    <div class="project-card reveal">
      <div class="project-card-header">
        <span class="project-type">FINTECH · RESEARCH</span>
        <span class="project-status status-active">ACTIVE</span>
      </div>
      <div class="project-body">
        <div class="project-title">DRL Crypto Trading Engine</div>
        <div class="project-desc">Deep reinforcement learning system for cryptocurrency trading. Integrates sentiment analysis, backtesting pipelines, and online/offline learning paradigms.</div>
        <div class="project-stack">
          <span class="stack-tag">PyTorch</span><span class="stack-tag">DRL</span>
          <span class="stack-tag">NLP</span><span class="stack-tag">Pandas</span>
        </div>
      </div>
    </div>
    <div class="project-card reveal">
      <div class="project-card-header">
        <span class="project-type">SAAS · STARTUP</span>
        <span class="project-status status-active">ACTIVE</span>
      </div>
      <div class="project-body">
        <div class="project-title">AI Booking Automation</div>
        <div class="project-desc">Engineered AI solutions to automate calls and bookings for tradies and NZ businesses. Full CTO build — from architecture to deployment.</div>
        <div class="project-stack">
          <span class="stack-tag">Next.js</span><span class="stack-tag">Python</span>
          <span class="stack-tag">LLM</span><span class="stack-tag">Twilio</span>
        </div>
      </div>
    </div>
    <div class="project-card reveal">
      <div class="project-card-header">
        <span class="project-type">MOBILE · SAAS</span>
        <span class="project-status status-build">BUILDING</span>
      </div>
      <div class="project-body">
        <div class="project-title">Timer App Startup</div>
        <div class="project-desc">iOS-first timer application with subscription monetisation via RevenueCat. Designed for focus, productivity, and athlete training flows.</div>
        <div class="project-stack">
          <span class="stack-tag">Swift</span><span class="stack-tag">iOS</span>
          <span class="stack-tag">RevenueCat</span><span class="stack-tag">StoreKit</span>
        </div>
      </div>
    </div>
    <div class="project-card reveal">
      <div class="project-card-header">
        <span class="project-type">WEB3</span>
        <span class="project-status status-done">SHIPPED</span>
      </div>
      <div class="project-body">
        <div class="project-title">Web3 DApp Projects</div>
        <div class="project-desc">Full-stack decentralised applications using React, Solidity, and Web3 frameworks. Smart contract integrations and blockchain-based UIs.</div>
        <div class="project-stack">
          <span class="stack-tag">Solidity</span><span class="stack-tag">React</span>
          <span class="stack-tag">Hardhat</span><span class="stack-tag">ethers.js</span>
        </div>
      </div>
    </div>
    <div class="project-card reveal" style="border-style:dashed; cursor:default">
      <div class="project-card-header">
        <span class="project-type">???</span>
        <span class="project-status status-build">STEALTH</span>
      </div>
      <div class="project-body">
        <div class="project-title" style="color:var(--muted)">[ REDACTED ]</div>
        <div class="project-desc">Something interesting is brewing. Stay tuned.</div>
        <div style="margin-top:24px; color:var(--muted); font-size:11px; font-style:italic">// access denied — clearance level insufficient</div>
      </div>
    </div>
  </div>
</section>

<!-- EXPERIENCE -->
<section id="experience">
  <div class="section-header reveal">
    <span class="section-num">04.</span>
    <h2 class="section-title"><span class="accent">git</span>_log --experience</h2>
    <div class="section-line"></div>
  </div>
  <div class="git-log">
    <div class="git-entry">
      <div class="git-hash">
        <span>commit a3f9c2d</span>
        <span class="git-date">Dec 2024 → present</span>
        <span style="background:rgba(57,255,107,0.1);color:var(--green);padding:1px 8px;font-size:10px;border:1px solid var(--green-dim)">HEAD → main</span>
      </div>
      <div class="git-title">CTO (Co-Founder)</div>
      <div class="git-org">Aialow.co · Full-time · Remote, NZ</div>
      <div class="git-body">Leading engineered solutions to automate calls and bookings for tradies and sole business owners in NZ. Building the full stack from LLM integration to customer-facing product.</div>
    </div>
    <div class="git-entry">
      <div class="git-hash">
        <span>commit b91e45a</span>
        <span class="git-date">Oct 2024 → present</span>
      </div>
      <div class="git-title">CTO (Co-Founder)</div>
      <div class="git-org">Alignwithblind.com · Part-time · Auckland</div>
      <div class="git-body">Leading AI-powered GP workflow automation for NZ clinics. Integrating LLM tooling into medical admin and triage pipelines.</div>
    </div>
    <div class="git-entry">
      <div class="git-hash">
        <span>commit f22d18b</span>
        <span class="git-date">Nov 2021 → 2026</span>
      </div>
      <div class="git-title">Research Assistant — AI Based Web Development</div>
      <div class="git-org">University of Auckland · Part-time</div>
      <div class="git-body">Working on AI-based web development, focusing on machine learning-heavy web applications and AI feature integrations.</div>
    </div>
    <div class="git-entry">
      <div class="git-hash">
        <span>commit 9cc7f01</span>
        <span class="git-date">Jan 2021 → 2026</span>
      </div>
      <div class="git-title">Bachelor of Computer Science (Honours)</div>
      <div class="git-org">University of Auckland</div>
      <div class="git-body">GPA 8.0831/9.0. Specialising in ML and large-scale software development. First Class Honours Scholar. Thesis: Privacy-preserving on-device AI for medical triage.</div>
    </div>
  </div>
</section>

<!-- TERMINAL PLAYGROUND -->
<section id="terminal-demo">
  <div class="section-header reveal">
    <span class="section-num">05.</span>
    <h2 class="section-title"><span class="accent">interactive</span>_terminal.sh</h2>
    <div class="section-line"></div>
  </div>
  <div class="big-terminal terminal-window reveal">
    <div class="terminal-bar">
      <div class="dot red"></div><div class="dot yellow"></div><div class="dot green"></div>
      <span class="terminal-title">brendan@portfolio:~ — ask me anything</span>
    </div>
    <div class="terminal-body" id="term-output">
      <div class="cmd-line"><span class="prompt">brendan@portfolio:~$</span><span class="cmd">./welcome.sh</span></div>
      <div class="output" style="color:var(--green); margin-top:8px">Welcome to Brendan's interactive terminal! 👋</div>
      <div class="output-dim">Type 'help' for available commands.</div>
      <div class="output-dim" style="margin-top:4px">Hint: try the Konami code for a surprise 👀</div>
      <br>
    </div>
    <div class="term-input-row">
      <span class="prompt">brendan@portfolio:~$</span>
      <input id="term-input" type="text" autocomplete="off" spellcheck="false" placeholder="type a command..." autofocus>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact">
  <div class="section-header reveal" style="justify-content:center; text-align:center;">
    <span class="section-num">06.</span>
    <h2 class="section-title"><span class="accent">ping</span>_me.sh</h2>
  </div>
  <div style="text-align:center">
    <pre class="contact-ascii" style="font-size:9px; display:inline-block; margin-bottom:32px">
  .-------.
  | [MSG] |=====> got an idea?  let's build it.
  '-------'
    </pre>
    <p style="color:var(--muted); font-size:13px; margin-bottom:40px; max-width:500px; margin-left:auto; margin-right:auto">
      Always open to collaborating on ambitious projects, research, or
      the right full-time role — especially <span style="color:var(--cyan)">fintech</span> or
      <span style="color:var(--green)">healthcare AI</span>.
    </p>
    <div class="contact-links">
      <a href="mailto:brendanms26@gmail.com" class="contact-link">
        <div class="contact-link-icon">✉</div>
        <span>email</span>
      </a>
      <a href="https://linkedin.com" target="_blank" class="contact-link">
        <div class="contact-link-icon">in</div>
        <span>linkedin</span>
      </a>
      <a href="https://github.com" target="_blank" class="contact-link">
        <div class="contact-link-icon">&lt;/&gt;</div>
        <span>github</span>
      </a>
      <a href="https://youtube.com" target="_blank" class="contact-link">
        <div class="contact-link-icon">▶</div>
        <span>youtube</span>
      </a>
    </div>
  </div>
</section>

<footer>
  <span style="color:var(--muted)">© 2026 Brendan Manseung Choi</span>
  <span style="color:var(--green); font-size:10px">// built with ♥ + too much caffeine</span>
  <span style="color:var(--muted)">Auckland, NZ 🥝</span>
</footer>

<script>
// ── Custom cursor ──
const cursor = document.getElementById('cursor');
document.addEventListener('mousemove', e => {
  cursor.style.left = e.clientX + 'px';
  cursor.style.top = e.clientY + 'px';
});

// ── Scroll reveal ──
const reveals = document.querySelectorAll('.reveal, .git-entry');
const observer = new IntersectionObserver(entries => {
  entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.1 });
reveals.forEach(el => observer.observe(el));

// ── Terminal commands ──
const commands = {
  help: () => `
<span style="color:var(--cyan)">Available commands:</span>
  <span style="color:var(--green)">about</span>       — who is brendan?
  <span style="color:var(--green)">skills</span>      — tech stack overview
  <span style="color:var(--green)">projects</span>    — list of projects
  <span style="color:var(--green)">contact</span>     — how to reach me
  <span style="color:var(--green)">hire</span>        — why you should hire me
  <span style="color:var(--green)">coffee</span>      — ☕
  <span style="color:var(--green)">clear</span>       — clear terminal
  <span style="color:var(--green)">matrix</span>      — 🕶️
  <span style="color:var(--green)">sudo hire me</span> — try it`,
  about: () => `<span style="color:var(--amber)">Brendan Manseung Choi</span>
Fullstack AI Engineer · Auckland, NZ
BSc(Hons) Computer Science · GPA 8.0831
CTO at 2 startups · Research Assistant · Personal Trainer
Interests: fintech, healthcare AI, on-device ML, web3`,
  skills: () => `<span style="color:var(--cyan)">Core skills:</span>
  🐍 Python, PyTorch, TensorFlow
  ⚛️  React, Next.js, Node.js
  📱 Swift, iOS, React Native
  🤖 LLMs, RAG, LoRA, DRL
  🌐 Web3, Solidity, ethers.js
  🐳 Docker, AWS, PostgreSQL`,
  projects: () => `<span style="color:var(--cyan)">Recent projects:</span>
  [DONE]   On-device Medical Triage AI (thesis)
  [ACTIVE] DRL Crypto Trading Engine
  [ACTIVE] AI Booking Automation SaaS
  [BUILD]  Timer App (iOS)
  [DONE]   Web3 DApp Collection
  [???]    [ REDACTED ]`,
  contact: () => `<span style="color:var(--green)">How to reach Brendan:</span>
  📧 brendanms26@gmail.com
  📱 0220508184
  📍 Auckland, New Zealand
  🔗 linkedin.com/in/brendan-choi`,
  hire: () => `<span style="color:var(--green)">Why hire Brendan?</span>
  ✓ Shipped production AI systems (not just toy projects)
  ✓ CTO experience — can own a full product
  ✓ Research + engineering combo (rare)
  ✓ Fast learner, self-starter, NZ timezone
  ✓ Passionate about fintech & healthcare AI
  
  <span style="color:var(--amber)">Currently open to: full-time roles, contracts, collaboration</span>`,
  coffee: () => `<span style="color:var(--amber)">
  ( (
   ) )
  ........
  |      |]
  \\      /   ☕ loading creativity...
   '----'    [ ████████████ ] 100%
  
  another one for the road.</span>`,
  matrix: () => { startMatrix(); return '<span style="color:var(--green)">Initiating... follow the white rabbit 🐇</span>'; },
  'sudo hire me': () => `<span style="color:var(--red)">[sudo]</span> password for recruiter: <span style="color:var(--green)">access granted ✓</span>
  
  <span style="color:var(--amber)">Congratulations. You have excellent taste.</span>
  Redirecting to brendanms26@gmail.com...`,
  clear: () => '__clear__',
};

const termOutput = document.getElementById('term-output');
const termInput = document.getElementById('term-input');

function addLine(html) {
  termOutput.insertAdjacentHTML('beforeend', `<div style="margin:4px 0">${html}</div>`);
  termOutput.scrollTop = termOutput.scrollHeight;
}

termInput.addEventListener('keydown', e => {
  if (e.key !== 'Enter') return;
  const val = termInput.value.trim().toLowerCase();
  if (!val) return;
  addLine(`<span style="color:var(--green)">brendan@portfolio:~$</span> <span style="color:var(--white)">${val}</span>`);
  const fn = commands[val];
  if (fn) {
    const out = fn();
    if (out === '__clear__') {
      termOutput.innerHTML = '';
    } else {
      addLine(`<span style="padding-left:16px; display:block">${out}</span>`);
    }
  } else {
    addLine(`<span style="color:var(--red); padding-left:16px">command not found: ${val} — try 'help'</span>`);
  }
  termInput.value = '';
});

// ── MATRIX RAIN EASTER EGG ──
const canvas = document.getElementById('matrix-canvas');
const ctx = canvas.getContext('2d');
let matrixInterval = null;

function startMatrix() {
  document.body.classList.add('matrix-active');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  const fontSize = 14;
  const cols = Math.floor(canvas.width / fontSize);
  const drops = Array(cols).fill(1);
  const chars = 'アイウエオカキクケコサシスセソタチツテトナニヌネノハヒフヘホ0123456789ABCDEF</>[]{}';
  matrixInterval = setInterval(() => {
    ctx.fillStyle = 'rgba(8,11,15,0.05)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = '#39ff6b';
    ctx.font = fontSize + 'px JetBrains Mono';
    drops.forEach((y, i) => {
      ctx.fillText(chars[Math.floor(Math.random() * chars.length)], i * fontSize, y * fontSize);
      if (y * fontSize > canvas.height && Math.random() > 0.975) drops[i] = 0;
      drops[i]++;
    });
  }, 33);
}

function exitMatrix() {
  document.body.classList.remove('matrix-active');
  clearInterval(matrixInterval);
  ctx.clearRect(0, 0, canvas.width, canvas.height);
}

// ── KONAMI CODE ──
const konami = ['ArrowUp','ArrowUp','ArrowDown','ArrowDown','ArrowLeft','ArrowRight','ArrowLeft','ArrowRight','b','a'];
let konamiIdx = 0;
document.addEventListener('keydown', e => {
  if (e.key === 'Escape') { exitMatrix(); return; }
  if (e.key === konami[konamiIdx]) {
    konamiIdx++;
    if (konamiIdx === konami.length) { startMatrix(); konamiIdx = 0; }
  } else { konamiIdx = 0; }
});

// ── GIT ENTRY ANIMATION ──
document.querySelectorAll('.git-entry').forEach((el, i) => {
  el.style.transitionDelay = (i * 0.1) + 's';
});
</script>
</body>
</html>